# Generated by scripts/dump_config_reference.py. Do not edit by hand.
#
# Every value below is the model default from src/local_first_agent_os/settings.py.
# Commented entries are derived at runtime (usually from your home directory) or
# have no default; uncomment only what you mean to override.
#
# See docs/configuration.md for what each one decides.

# --- Feature flags ---------------------------------------------

# Whether an access-control refusal stops the action ('enforcing') or is
# recorded while the action proceeds ('observing'). 'observing' exists so that
# young rules cannot cost an operator a manual test run; it still runs every
# check and logs each refusal it declined to make. It never relaxes the
# irreversible capabilities in access_posture.ALWAYS_ENFORCED, and the process
# says which posture it is in at startup and on /health.
LOCAL_AGENT_ACCESS_POSTURE=enforcing

# Whether a dispatched frontier agent is handed a read-only MCP view of the
# coordination ledger: what has executed, who owns the resident loops, and
# what dispatch intents exist. The startup skill says execution history is a
# query rather than an inference, and until this existed a dispatched agent
# had no way to run that query and inferred instead. Read-only by
# construction: no verb it exposes can write, so an agent cannot file evidence
# about its own run. Off returns the spawn to no MCP configuration at all. See
# docs/agent_ledger_read_access_design.md.
LOCAL_AGENT_AGENT_LEDGER_READ_ACCESS=true

# Whether the agent drives an already-running browser with your logged-in
# sessions, attaches to a named URL, or launches a throwaway one.
#
# How the MCP server reaches a browser. "auto_connect" attaches to an already
# running eligible Chrome (144+, remote debugging enabled), "browser_url"
# attaches to an explicit debugging endpoint, and "launch" starts a dedicated
# instance with chrome_devtools_launch_args. Read-only actions never launch a
# browser profile in the attach modes.
LOCAL_AGENT_CHROME_DEVTOOLS_ATTACH_MODE=auto_connect

# Let an observational action start a browser, rather than failing and
# requiring an explicit start. Decides failure semantics the operator sees.
#
# Observational actions may lazily start the supervised MCP process when it is
# cleanly stopped; a FAILED generation always requires an explicit /chrome
# start so failures never trigger automatic restarts.
LOCAL_AGENT_CHROME_DEVTOOLS_LAZY_START=true

# Which Chrome DevTools implementation to drive. The cli path exists as a
# diagnostic fallback; its honest end state is deletion, not documentation.
LOCAL_AGENT_CHROME_DEVTOOLS_TRANSPORT=mcp

# Whether this application's own coordination commands cross a process
# boundary. They always did, because the only transport the factory could
# build was a subprocess, and the subprocess re-executes the packaged CLI: a
# fresh interpreter re-importing the same package the caller already has
# loaded, to call a function it could have called directly. Measured
# 2026-07-31 against the live Postgres, `list_sagas` took 0.418s that way and
# 0.007s in process. The stdio bug that `pi_handoff_to_daemon` works around is
# downstream of the same fork: a child spawned from a resident daemon inherits
# stdio the daemon no longer owns. `subprocess` remains selectable, and is
# still the right answer for an external agent calling this repository over
# MCP - that one is a separate program and the boundary is real.
LOCAL_AGENT_COORDINATION_TRANSPORT=in_process

# Whether ledger events are delivered to an external consumer. A discriminated
# union, so the disabled case cannot carry a half-filled consumer and topic.
#
# The durable outbox is operational delivery state, not an audit log. Keep it
# disabled unless a named consumer and topic make every PENDING row
# actionable. events.jsonl remains the local human-readable audit trail.
# LOCAL_AGENT_LEDGER_OUTBOX=  # derived at runtime

# Run the tracemalloc collector. The test suite forces this off, because a
# live collector turns an ordinary run into a profiling workload.
LOCAL_AGENT_MEMORY_PROFILING_ENABLED=false

# Replace real inference with canned responses. Changes every downstream
# output, so it is a behaviour switch rather than wiring.
LOCAL_AGENT_MOCK_MODELS=false

# Export OpenTelemetry traces to otel_traces_endpoint.
LOCAL_AGENT_OTEL_TRACES_ENABLED=false

# When the pi daemon is unreachable, run in-process instead of failing.
# Distinct from pi_handoff_to_daemon, which decides whether to try the daemon
# at all; this one only covers the daemon being unreachable.
LOCAL_AGENT_PI_DIRECT_FALLBACK=false

# Hand a pi query to the resident pi-daemon on 127.0.0.1 rather than running
# it in this process. Not about which agent CLI answers. Set false for the
# documented workaround to the daemon's Bad file descriptor bug;
# LOCAL_AGENT_PI_FORCE_DIRECT=1 is the legacy spelling.
#
# Whether `pi` hands a query to the resident pi-daemon or runs it here in the
# foreground. The daemon listens on 127.0.0.1, so this is a same-machine
# handoff and has nothing to do with which agent CLI eventually answers: Codex
# and Claude Code are reached the same way down either path. Named for the
# working state rather than the mechanism; the old LOCAL_AGENT_PI_FORCE_DIRECT
# spelled the same decision as an internal knob, inverted, and is still
# honoured because every handoff doc that works around the daemon's
# `init_sys_streams` bug tells an operator to set it.
LOCAL_AGENT_PI_HANDOFF_TO_DAEMON=true

# Send continuous profiles to pyroscope_server_address.
LOCAL_AGENT_PYROSCOPE_ENABLED=false

LOCAL_AGENT_SAGA_EXECUTOR=cli

# Execute pi work through DBOS rather than in-process. Changes durability and
# recovery semantics an operator can observe. A boolean where
# saga_executor_backend shows the better shape: this wants to become
# pi_execution_backend with named states.
LOCAL_AGENT_USE_DBOS=false

# Record intended Workflowy writes without performing them. Defaulting to true
# means the shipped default is the non-writing product.
LOCAL_AGENT_WORKFLOWY_DRY_RUN=true

# --- Everything else ------------------------------------------

LOCAL_AGENT_APP_NAME=local_first_agent_os

LOCAL_AGENT_APPLE_NOTES_FETCH_SCRIPT=

LOCAL_AGENT_APPLICATION_VERSION=0.1.0

LOCAL_AGENT_ARTIFACT_BACKEND=filesystem

# LOCAL_AGENT_ARTIFACT_ROOT=  # derived at runtime

LOCAL_AGENT_ARTIFACT_WRITE_TIMEOUT_SECONDS=60

# LOCAL_AGENT_ASR_TRIGGERS_PATH=  # derived at runtime

# A cold isolated-profile Chrome launch takes ~9s idle and much longer under
# host load (proven 2026-07-17), so attach gets a wider budget.
LOCAL_AGENT_CHROME_DEVTOOLS_ATTACH_TIMEOUT_SECONDS=45.0

# Whether the CLI transport (the diagnostic fallback) starts Chrome itself.
# Only consulted when chrome_devtools_transport is "cli".
LOCAL_AGENT_CHROME_DEVTOOLS_AUTO_START=true

LOCAL_AGENT_CHROME_DEVTOOLS_BROWSER_URL=

LOCAL_AGENT_CHROME_DEVTOOLS_CALL_TIMEOUT_SECONDS=60.0

LOCAL_AGENT_CHROME_DEVTOOLS_COMMAND=npx

# Pinned Chrome DevTools MCP implementation. The live MCP path refuses to
# resolve a floating "@latest" spec; bump the pin deliberately after a host
# preflight proves the new version.
# LOCAL_AGENT_CHROME_DEVTOOLS_COMMAND_ARGS=  # derived at runtime

# Stop the supervised process after this much inactivity; 0 disables.
LOCAL_AGENT_CHROME_DEVTOOLS_IDLE_SHUTDOWN_SECONDS=900.0

# LOCAL_AGENT_CHROME_DEVTOOLS_LAUNCH_ARGS=  # derived at runtime

LOCAL_AGENT_CHROME_DEVTOOLS_LOG_TAIL_CHARS=4000

# LOCAL_AGENT_CHROME_DEVTOOLS_START_ARGS=  # derived at runtime

# Phase-specific MCP deadlines.
LOCAL_AGENT_CHROME_DEVTOOLS_STARTUP_TIMEOUT_SECONDS=20.0

LOCAL_AGENT_CHROME_DEVTOOLS_STOP_TIMEOUT_SECONDS=5.0

# How long a CLI-transport command may run before it is abandoned.
LOCAL_AGENT_CHROME_DEVTOOLS_TIMEOUT_SECONDS=60

# LOCAL_AGENT_CONFIG_DIR=  # derived at runtime

LOCAL_AGENT_COORDINATION_BACKEND=postgres

LOCAL_AGENT_COORDINATION_COMMAND_TIMEOUT_SECONDS=30

LOCAL_AGENT_COORDINATION_DATABASE_URL=

# LOCAL_AGENT_COORDINATION_ROOT=  # derived at runtime

# LOCAL_AGENT_CORS_ORIGINS=  # derived at runtime

LOCAL_AGENT_DATABASE_URL=postgresql+psycopg://postgres:postgres@127.0.0.1:5432/local_agent

# DBOS's built-in Python admin server binds 0.0.0.0 and offers no host
# setting. Keep it off unless a deliberate network boundary isolates it.
LOCAL_AGENT_DBOS_ADMIN_SERVER_ENABLED=false

DBOS_CONDUCTOR_EXECUTOR_METADATA=

LOCAL_AGENT_DBOS_CONDUCTOR_KEY=

LOCAL_AGENT_DBOS_CONDUCTOR_URL=

LOCAL_AGENT_DBOS_EXECUTOR_ID=

LOCAL_AGENT_DBOS_SYSTEM_DATABASE_URL=

LOCAL_AGENT_ENV=local

LOCAL_AGENT_GIT_OPERATION_TIMEOUT_SECONDS=30

# The least-privileged way into the DBOS system database: a role granted the
# two metadata columns the execution-ledger tallies read and nothing else. A
# process given this URL instead of the admin one cannot reach a workflow's
# inputs even if its code asks. See scripts/grant_execution_ledger_reader.sql.
LOCAL_AGENT_LEDGER_READER_DATABASE_URL=

# LOCAL_AGENT_LIFECYCLE_LOG_DIR=  # derived at runtime

LOCAL_AGENT_LIFECYCLE_LOG_MAX_BYTES=52428800

LOCAL_AGENT_LIFECYCLE_LOG_RETAINED_TAIL_BYTES=5242880

# LOCAL_AGENT_LIFECYCLE_MAINTENANCE_STATE_PATH=  # derived at runtime

# How long scheduled maintenance keeps collectable audit evidence: execution
# transcripts and artifacts not pinned by a checkpoint, terminal leases and
# dispatch intents nothing still references, settled ledger events, notes, and
# handoffs. ``None`` keeps them forever. Durable evidence in
# ``task_artifacts`` and the saga tables is never in scope here; deleting a
# project's evidence is an operator decision, not a scheduled one.
LOCAL_AGENT_LIFECYCLE_RETENTION_SECONDS=7776000

LOCAL_AGENT_LLAMA_BASE_URL=http://127.0.0.1:8080

# LOCAL_AGENT_LLAMA_MODELS_DIR=  # derived at runtime

LOCAL_AGENT_LOG_LEVEL=INFO

LOCAL_AGENT_MEMORY_PROFILE_SAMPLE_EVERY=20

LOCAL_AGENT_MEMORY_PROFILE_TOP_N=8

LOCAL_AGENT_MINIO_ACCESS_KEY=localagent

LOCAL_AGENT_MINIO_ARTIFACT_BUCKET=local-agent-artifacts

LOCAL_AGENT_MINIO_ENDPOINT=127.0.0.1:9000

LOCAL_AGENT_MINIO_SECRET_KEY=localagent-secret

LOCAL_AGENT_MINIO_SECURE=false

LOCAL_AGENT_OTEL_TRACES_ENDPOINT=http://127.0.0.1:4318/v1/traces

LOCAL_AGENT_OTEL_TRACES_EXPORT_TIMEOUT_SECONDS=2.0

# LOCAL_AGENT_OTEL_TRACES_HEADERS=  # derived at runtime

LOCAL_AGENT_PI_DAEMON_AUTOSTART=true

LOCAL_AGENT_PI_DAEMON_HOST=127.0.0.1

LOCAL_AGENT_PI_DAEMON_PORT=8766

LOCAL_AGENT_PI_DAEMON_URL=

LOCAL_AGENT_PI_STREAM_HEARTBEAT_SECONDS=10

LOCAL_AGENT_PI_STREAM_IDLE_TIMEOUT_SECONDS=30

LOCAL_AGENT_PROGRESS_ASSESSMENT_TIMEOUT_SECONDS=300

# LOCAL_AGENT_PROJECTS_ROOT=  # derived at runtime

LOCAL_AGENT_PYROSCOPE_SAMPLE_RATE=100

LOCAL_AGENT_PYROSCOPE_SERVER_ADDRESS=http://127.0.0.1:4040

LOCAL_AGENT_REFINERY_POLL_SECONDS=15.0

LOCAL_AGENT_SAGA_MAX_REVIEW_ROUNDS=4

LOCAL_AGENT_SAGA_TASK_TIMEOUT_SECONDS=7200

# LOCAL_AGENT_SAGA_WORKTREE_ROOT=  # derived at runtime

LOCAL_AGENT_SERVICE_NAME=local-agent

# LOCAL_AGENT_SESSION_CONTEXT_EXPORT_DIR=  # derived at runtime

LOCAL_AGENT_SESSION_DAEMON_HOST=127.0.0.1

LOCAL_AGENT_SESSION_DAEMON_PORT=8765

# LOCAL_AGENT_SPOOL_DIR=  # derived at runtime

LOCAL_AGENT_STREAM_DRAIN_TIMEOUT_SECONDS=10

LOCAL_AGENT_STRUCTURED_LOGS=true

LOCAL_AGENT_WEB_DIST=web/dist

LOCAL_AGENT_WHISPER_ACTIVE_MODEL=ggml-large-v3-turbo.bin

# LOCAL_AGENT_WHISPER_BIN_PATH=  # derived at runtime

LOCAL_AGENT_WHISPER_FLASH_ATTN=true

LOCAL_AGENT_WHISPER_HOST=127.0.0.1

LOCAL_AGENT_WHISPER_IDLE_MODEL=ggml-base.en.bin

# LOCAL_AGENT_WHISPER_MODELS_DIR=  # derived at runtime

LOCAL_AGENT_WHISPER_PORT=8090

# --- whisper.cpp (ASR backend) -------------------------------------------
# Background runtime kept resident by scripts/start-agent-runtime.sh on a port
# distinct from llama-server. The registry can define the active ASR model and
# backend capabilities; these settings remain fallback/default process knobs
# for the whisper-server binary and idle-model swap.
LOCAL_AGENT_WHISPER_BASE_URL=

LOCAL_AGENT_WHISPER_THREADS=8

WF_API_KEY=

LOCAL_AGENT_WORKFLOWY_FETCH_SCRIPT=

LOCAL_AGENT_WORKFLOWY_INSERT_SCRIPT=
