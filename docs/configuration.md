# Configuration reference

<!-- Generated by scripts/dump_config_reference.py. Do not edit by hand. -->

Every value the application reads comes from `src/local_first_agent_os/settings.py`.
This file is generated from that model, so it cannot drift from it; `--check` fails when it has.

## Feature flags

These change what the product does, as opposed to where it points or how fast it runs.
A field is listed here when it carries `feature_flag` in its `json_schema_extra`, so this set is derived rather than curated.

| Setting | Environment | States | Default | What it decides |
| --- | --- | --- | --- | --- |
| `access_posture` | `LOCAL_AGENT_ACCESS_POSTURE` | `AccessPosture` | `enforcing` | Whether an access-control refusal stops the action ('enforcing') or is recorded while the action proceeds ('observing'). 'observing' exists so that young rules cannot cost an operator a manual test run; it still runs every check and logs each refusal it declined to make. It never relaxes the irreversible capabilities in access_posture.ALWAYS_ENFORCED, and the process says which posture it is in at startup and on /health. |
| `agent_ledger_read_access` | `LOCAL_AGENT_AGENT_LEDGER_READ_ACCESS` | `bool` | `True` | Whether a dispatched frontier agent is handed a read-only MCP view of the coordination ledger: what has executed, who owns the resident loops, and what dispatch intents exist. The startup skill says execution history is a query rather than an inference, and until this existed a dispatched agent had no way to run that query and inferred instead. Read-only by construction: no verb it exposes can write, so an agent cannot file evidence about its own run. Off returns the spawn to no MCP configuration at all. See docs/completed/agent_ledger_read_access_design.md. |
| `chrome_devtools_attach_mode` | `LOCAL_AGENT_CHROME_DEVTOOLS_ATTACH_MODE` | `Literal` | `auto_connect` | Whether the agent drives an already-running browser with your logged-in sessions, attaches to a named URL, or launches a throwaway one. |
| `chrome_devtools_lazy_start` | `LOCAL_AGENT_CHROME_DEVTOOLS_LAZY_START` | `bool` | `True` | Let an observational action start a browser, rather than failing and requiring an explicit start. Decides failure semantics the operator sees. |
| `chrome_devtools_transport` | `LOCAL_AGENT_CHROME_DEVTOOLS_TRANSPORT` | `Literal` | `mcp` | Which Chrome DevTools implementation to drive. The cli path exists as a diagnostic fallback; its honest end state is deletion, not documentation. |
| `coordination_transport` | `LOCAL_AGENT_COORDINATION_TRANSPORT`<br>`AGENT_COORDINATION_TRANSPORT` | `CoordinationTransportKind` | `in_process` | _undescribed_ |
| `ledger_outbox` | `LOCAL_AGENT_LEDGER_OUTBOX` | `DisabledLedgerOutbox \| ConfiguredLedgerOutbox` | _derived_ | Whether ledger events are delivered to an external consumer. A discriminated union, so the disabled case cannot carry a half-filled consumer and topic. |
| `memory_profiling_enabled` | `LOCAL_AGENT_MEMORY_PROFILING_ENABLED` | `bool` | `False` | Run the tracemalloc collector. The test suite forces this off, because a live collector turns an ordinary run into a profiling workload. |
| `mock_models` | `LOCAL_AGENT_MOCK_MODELS` | `bool` | `False` | Replace real inference with canned responses. Changes every downstream output, so it is a behaviour switch rather than wiring. |
| `otel_traces_enabled` | `LOCAL_AGENT_OTEL_TRACES_ENABLED` | `bool` | `False` | Export OpenTelemetry traces to otel_traces_endpoint. |
| `pi_direct_fallback` | `LOCAL_AGENT_PI_DIRECT_FALLBACK` | `bool` | `False` | When the pi daemon is unreachable, run in-process instead of failing. Distinct from pi_handoff_to_daemon, which decides whether to try the daemon at all; this one only covers the daemon being unreachable. |
| `pi_handoff_to_daemon` | `LOCAL_AGENT_PI_HANDOFF_TO_DAEMON` | `bool` | `True` | Hand a pi query to the resident pi-daemon on 127.0.0.1 rather than running it in this process. Not about which agent CLI answers. Set false for the documented workaround to the daemon's Bad file descriptor bug; LOCAL_AGENT_PI_FORCE_DIRECT=1 is the legacy spelling. |
| `pyroscope_enabled` | `LOCAL_AGENT_PYROSCOPE_ENABLED` | `bool` | `False` | Send continuous profiles to pyroscope_server_address. |
| `saga_executor_backend` | `LOCAL_AGENT_SAGA_EXECUTOR`<br>`LOCAL_AGENT_SAGA_EXECUTOR_BACKEND` | `Literal` | `cli` | _undescribed_ |
| `use_dbos` | `LOCAL_AGENT_USE_DBOS` | `bool` | `False` | Execute pi work through DBOS rather than in-process. Changes durability and recovery semantics an operator can observe. A boolean where saga_executor_backend shows the better shape: this wants to become pi_execution_backend with named states. |
| `workflowy_dry_run` | `LOCAL_AGENT_WORKFLOWY_DRY_RUN` | `bool` | `True` | Record intended Workflowy writes without performing them. Defaulting to true means the shipped default is the non-writing product. |

## Everything else

Endpoints, paths, credentials, and tuning values. Not flags: changing one moves the product, it does not alter what the product is.

### ASR

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `whisper_active_model` | `LOCAL_AGENT_WHISPER_ACTIVE_MODEL` | `ggml-large-v3-turbo.bin` | - |
| `whisper_bin_path` | `LOCAL_AGENT_WHISPER_BIN_PATH` | _derived_ | - |
| `whisper_flash_attn` | `LOCAL_AGENT_WHISPER_FLASH_ATTN` | `True` | - |
| `whisper_host` | `LOCAL_AGENT_WHISPER_HOST` | `127.0.0.1` | - |
| `whisper_idle_model` | `LOCAL_AGENT_WHISPER_IDLE_MODEL` | `ggml-base.en.bin` | - |
| `whisper_models_dir` | `LOCAL_AGENT_WHISPER_MODELS_DIR` | _derived_ | - |
| `whisper_port` | `LOCAL_AGENT_WHISPER_PORT` | `8090` | - |
| `whisper_server_url` | `LOCAL_AGENT_WHISPER_BASE_URL` | `None` | - |
| `whisper_threads` | `LOCAL_AGENT_WHISPER_THREADS` | `8` | - |

### Artifact storage

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `minio_access_key` | `LOCAL_AGENT_MINIO_ACCESS_KEY` | `localagent` | - |
| `minio_artifact_bucket` | `LOCAL_AGENT_MINIO_ARTIFACT_BUCKET` | `local-agent-artifacts` | - |
| `minio_endpoint` | `LOCAL_AGENT_MINIO_ENDPOINT` | `127.0.0.1:9000` | - |
| `minio_secret_key` | `LOCAL_AGENT_MINIO_SECRET_KEY` | `localagent-secret` | - |
| `minio_secure` | `LOCAL_AGENT_MINIO_SECURE` | `False` | - |

### Chrome

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `chrome_devtools_attach_timeout_seconds` | `LOCAL_AGENT_CHROME_DEVTOOLS_ATTACH_TIMEOUT_SECONDS` | `45.0` | - |
| `chrome_devtools_auto_start` | `LOCAL_AGENT_CHROME_DEVTOOLS_AUTO_START` | `True` | - |
| `chrome_devtools_browser_url` | `LOCAL_AGENT_CHROME_DEVTOOLS_BROWSER_URL` | `None` | - |
| `chrome_devtools_call_timeout_seconds` | `LOCAL_AGENT_CHROME_DEVTOOLS_CALL_TIMEOUT_SECONDS` | `60.0` | - |
| `chrome_devtools_command` | `LOCAL_AGENT_CHROME_DEVTOOLS_COMMAND` | `npx` | - |
| `chrome_devtools_command_args` | `LOCAL_AGENT_CHROME_DEVTOOLS_COMMAND_ARGS` | _derived_ | - |
| `chrome_devtools_idle_shutdown_seconds` | `LOCAL_AGENT_CHROME_DEVTOOLS_IDLE_SHUTDOWN_SECONDS` | `900.0` | - |
| `chrome_devtools_launch_args` | `LOCAL_AGENT_CHROME_DEVTOOLS_LAUNCH_ARGS` | _derived_ | - |
| `chrome_devtools_log_tail_chars` | `LOCAL_AGENT_CHROME_DEVTOOLS_LOG_TAIL_CHARS` | `4000` | - |
| `chrome_devtools_start_args` | `LOCAL_AGENT_CHROME_DEVTOOLS_START_ARGS` | _derived_ | - |
| `chrome_devtools_startup_timeout_seconds` | `LOCAL_AGENT_CHROME_DEVTOOLS_STARTUP_TIMEOUT_SECONDS` | `20.0` | - |
| `chrome_devtools_stop_timeout_seconds` | `LOCAL_AGENT_CHROME_DEVTOOLS_STOP_TIMEOUT_SECONDS` | `5.0` | - |
| `chrome_devtools_timeout_seconds` | `LOCAL_AGENT_CHROME_DEVTOOLS_TIMEOUT_SECONDS` | `60` | - |

### Coordination ledger

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `coordination_backend` | `LOCAL_AGENT_COORDINATION_BACKEND`<br>`AGENT_COORDINATION_BACKEND` | `postgres` | - |
| `coordination_command_timeout_seconds` | `LOCAL_AGENT_COORDINATION_COMMAND_TIMEOUT_SECONDS` | `30` | - |
| `coordination_database_url` | `LOCAL_AGENT_COORDINATION_DATABASE_URL`<br>`AGENT_COORDINATION_DATABASE_URL` | `None` | - |
| `coordination_root` | `LOCAL_AGENT_COORDINATION_ROOT` | _derived_ | - |

### DBOS

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `dbos_admin_server_enabled` | `LOCAL_AGENT_DBOS_ADMIN_SERVER_ENABLED` | `False` | - |
| `dbos_conductor_executor_metadata` | `DBOS_CONDUCTOR_EXECUTOR_METADATA`<br>`LOCAL_AGENT_DBOS_CONDUCTOR_EXECUTOR_METADATA` | `None` | - |
| `dbos_conductor_key` | `LOCAL_AGENT_DBOS_CONDUCTOR_KEY`<br>`DBOS_CONDUCTOR_KEY` | `None` | - |
| `dbos_conductor_url` | `LOCAL_AGENT_DBOS_CONDUCTOR_URL`<br>`DBOS_CONDUCTOR_URL` | `None` | - |
| `dbos_executor_id` | `LOCAL_AGENT_DBOS_EXECUTOR_ID`<br>`DBOS__VMID` | `None` | - |
| `dbos_system_database_url` | `LOCAL_AGENT_DBOS_SYSTEM_DATABASE_URL`<br>`DBOS_SYSTEM_DATABASE_URL` | `None` | - |

### General

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `app_name` | `LOCAL_AGENT_APP_NAME`<br>`DBOS_APPLICATION_NAME` | `local_first_agent_os` | - |
| `apple_notes_fetch_script` | `LOCAL_AGENT_APPLE_NOTES_FETCH_SCRIPT` | `None` | - |
| `application_version` | `LOCAL_AGENT_APPLICATION_VERSION`<br>`DBOS__APPVERSION` | `0.1.0` | - |
| `artifact_backend` | `LOCAL_AGENT_ARTIFACT_BACKEND` | `filesystem` | - |
| `artifact_root` | `LOCAL_AGENT_ARTIFACT_ROOT` | _derived_ | - |
| `artifact_write_timeout_seconds` | `LOCAL_AGENT_ARTIFACT_WRITE_TIMEOUT_SECONDS` | `60` | - |
| `asr_triggers_path` | `LOCAL_AGENT_ASR_TRIGGERS_PATH` | _derived_ | - |
| `config_dir` | `LOCAL_AGENT_CONFIG_DIR` | _derived_ | - |
| `cors_origins` | `LOCAL_AGENT_CORS_ORIGINS` | _derived_ | - |
| `database_url` | `LOCAL_AGENT_DATABASE_URL` | `postgresql+psycopg://postgres:postgres@127.0.0.1:5432/local_agent` | - |
| `env` | `LOCAL_AGENT_ENV` | `local` | - |
| `git_operation_timeout_seconds` | `LOCAL_AGENT_GIT_OPERATION_TIMEOUT_SECONDS` | `30` | - |
| `log_level` | `LOCAL_AGENT_LOG_LEVEL` | `INFO` | - |
| `progress_assessment_timeout_seconds` | `LOCAL_AGENT_PROGRESS_ASSESSMENT_TIMEOUT_SECONDS` | `300` | - |
| `projects_root` | `LOCAL_AGENT_PROJECTS_ROOT` | _derived_ | - |
| `refinery_poll_seconds` | `LOCAL_AGENT_REFINERY_POLL_SECONDS` | `15.0` | - |
| `service_name` | `LOCAL_AGENT_SERVICE_NAME` | `local-agent` | - |
| `session_context_export_dir` | `LOCAL_AGENT_SESSION_CONTEXT_EXPORT_DIR` | _derived_ | - |
| `session_daemon_host` | `LOCAL_AGENT_SESSION_DAEMON_HOST` | `127.0.0.1` | - |
| `session_daemon_port` | `LOCAL_AGENT_SESSION_DAEMON_PORT` | `8765` | - |
| `spool_dir` | `LOCAL_AGENT_SPOOL_DIR` | _derived_ | - |
| `stream_drain_timeout_seconds` | `LOCAL_AGENT_STREAM_DRAIN_TIMEOUT_SECONDS` | `10` | - |
| `structured_logs` | `LOCAL_AGENT_STRUCTURED_LOGS` | `True` | - |
| `web_dist` | `LOCAL_AGENT_WEB_DIST` | `web/dist` | - |

### Ledger

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `ledger_reader_database_url` | `LOCAL_AGENT_LEDGER_READER_DATABASE_URL` | `None` | - |

### Lifecycle maintenance

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `lifecycle_log_dir` | `LOCAL_AGENT_LIFECYCLE_LOG_DIR` | _derived_ | - |
| `lifecycle_log_max_bytes` | `LOCAL_AGENT_LIFECYCLE_LOG_MAX_BYTES` | `52428800` | - |
| `lifecycle_log_retained_tail_bytes` | `LOCAL_AGENT_LIFECYCLE_LOG_RETAINED_TAIL_BYTES` | `5242880` | - |
| `lifecycle_maintenance_state_path` | `LOCAL_AGENT_LIFECYCLE_MAINTENANCE_STATE_PATH` | _derived_ | - |
| `lifecycle_retention_seconds` | `LOCAL_AGENT_LIFECYCLE_RETENTION_SECONDS` | `7776000` | - |

### Local models

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `llama_base_url` | `LOCAL_AGENT_LLAMA_BASE_URL` | `http://127.0.0.1:8080` | - |
| `llama_models_dir` | `LOCAL_AGENT_LLAMA_MODELS_DIR` | _derived_ | - |

### Observability

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `memory_profile_sample_every` | `LOCAL_AGENT_MEMORY_PROFILE_SAMPLE_EVERY` | `20` | - |
| `memory_profile_top_n` | `LOCAL_AGENT_MEMORY_PROFILE_TOP_N` | `8` | - |
| `otel_traces_endpoint` | `LOCAL_AGENT_OTEL_TRACES_ENDPOINT` | `http://127.0.0.1:4318/v1/traces` | - |
| `otel_traces_export_timeout_seconds` | `LOCAL_AGENT_OTEL_TRACES_EXPORT_TIMEOUT_SECONDS` | `2.0` | - |
| `otel_traces_headers` | `LOCAL_AGENT_OTEL_TRACES_HEADERS` | _derived_ | - |
| `pyroscope_sample_rate` | `LOCAL_AGENT_PYROSCOPE_SAMPLE_RATE` | `100` | - |
| `pyroscope_server_address` | `LOCAL_AGENT_PYROSCOPE_SERVER_ADDRESS` | `http://127.0.0.1:4040` | - |

### Pi daemon

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `pi_daemon_autostart` | `LOCAL_AGENT_PI_DAEMON_AUTOSTART` | `True` | - |
| `pi_daemon_host` | `LOCAL_AGENT_PI_DAEMON_HOST` | `127.0.0.1` | - |
| `pi_daemon_port` | `LOCAL_AGENT_PI_DAEMON_PORT` | `8766` | - |
| `pi_daemon_url` | `LOCAL_AGENT_PI_DAEMON_URL` | `None` | - |
| `pi_stream_heartbeat_seconds` | `LOCAL_AGENT_PI_STREAM_HEARTBEAT_SECONDS` | `10` | - |
| `pi_stream_idle_timeout_seconds` | `LOCAL_AGENT_PI_STREAM_IDLE_TIMEOUT_SECONDS` | `30` | - |

### Saga execution

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `saga_max_review_rounds` | `LOCAL_AGENT_SAGA_MAX_REVIEW_ROUNDS` | `4` | - |
| `saga_task_timeout_seconds` | `LOCAL_AGENT_SAGA_TASK_TIMEOUT_SECONDS` | `7200` | - |
| `saga_worktree_root` | `LOCAL_AGENT_SAGA_WORKTREE_ROOT` | _derived_ | - |

### Workflowy

| Setting | Environment | Default | Notes |
| --- | --- | --- | --- |
| `workflowy_api_key` | `WF_API_KEY` | `None` | - |
| `workflowy_fetch_script` | `LOCAL_AGENT_WORKFLOWY_FETCH_SCRIPT` | `None` | - |
| `workflowy_insert_script` | `LOCAL_AGENT_WORKFLOWY_INSERT_SCRIPT` | `None` | - |

## Why these exist

Read from the comments above each field in `settings.py`, so the reasoning lives next to the code and reaches this file without being written twice. A setting missing here has no comment explaining it yet.

**`chrome_devtools_attach_mode`** _(flag)_

How the MCP server reaches a browser. "auto_connect" attaches to an already running eligible Chrome (144+, remote debugging enabled), "browser_url" attaches to an explicit debugging endpoint, and "launch" starts a dedicated instance with chrome_devtools_launch_args. Read-only actions never launch a browser profile in the attach modes.

**`chrome_devtools_attach_timeout_seconds`**

A cold isolated-profile Chrome launch takes ~9s idle and much longer under host load (proven 2026-07-17), so attach gets a wider budget.

**`chrome_devtools_auto_start`**

Whether the CLI transport (the diagnostic fallback) starts Chrome itself. Only consulted when chrome_devtools_transport is "cli".

**`chrome_devtools_command_args`**

Pinned Chrome DevTools MCP implementation. The live MCP path refuses to resolve a floating "@latest" spec; bump the pin deliberately after a host preflight proves the new version.

**`chrome_devtools_idle_shutdown_seconds`**

Stop the supervised process after this much inactivity; 0 disables.

**`chrome_devtools_lazy_start`** _(flag)_

Observational actions may lazily start the supervised MCP process when it is cleanly stopped; a FAILED generation always requires an explicit /chrome start so failures never trigger automatic restarts.

**`chrome_devtools_startup_timeout_seconds`**

Phase-specific MCP deadlines.

**`chrome_devtools_timeout_seconds`**

How long a CLI-transport command may run before it is abandoned.

**`coordination_transport`** _(flag)_

Whether this application's own coordination commands cross a process boundary. They always did, because the only transport the factory could build was a subprocess, and the subprocess re-executes the packaged CLI: a fresh interpreter re-importing the same package the caller already has loaded, to call a function it could have called directly. Measured 2026-07-31 against the live Postgres, `list_sagas` took 0.418s that way and 0.007s in process. The stdio bug that `pi_handoff_to_daemon` works around is downstream of the same fork: a child spawned from a resident daemon inherits stdio the daemon no longer owns. `subprocess` remains selectable, and is still the right answer for an external agent calling this repository over MCP - that one is a separate program and the boundary is real.

**`dbos_admin_server_enabled`**

DBOS's built-in Python admin server binds 0.0.0.0 and offers no host setting. Keep it off unless a deliberate network boundary isolates it.

**`ledger_outbox`** _(flag)_

The durable outbox is operational delivery state, not an audit log. Keep it disabled unless a named consumer and topic make every PENDING row actionable. events.jsonl remains the local human-readable audit trail.

**`ledger_reader_database_url`**

The least-privileged way into the DBOS system database: a role granted the two metadata columns the execution-ledger tallies read and nothing else. A process given this URL instead of the admin one cannot reach a workflow's inputs even if its code asks. See scripts/grant_execution_ledger_reader.sql.

**`lifecycle_retention_seconds`**

How long scheduled maintenance keeps collectable audit evidence: execution transcripts and artifacts not pinned by a checkpoint, terminal leases and dispatch intents nothing still references, settled ledger events, notes, and handoffs.  ``None`` keeps them forever.  Durable evidence in ``task_artifacts`` and the saga tables is never in scope here; deleting a project's evidence is an operator decision, not a scheduled one.

**`pi_handoff_to_daemon`** _(flag)_

Whether `pi` hands a query to the resident pi-daemon or runs it here in the foreground. The daemon listens on 127.0.0.1, so this is a same-machine handoff and has nothing to do with which agent CLI eventually answers: Codex and Claude Code are reached the same way down either path. Named for the working state rather than the mechanism; the old LOCAL_AGENT_PI_FORCE_DIRECT spelled the same decision as an internal knob, inverted, and is still honoured because every handoff doc that works around the daemon's `init_sys_streams` bug tells an operator to set it.

**`whisper_server_url`**

--- whisper.cpp (ASR backend) ------------------------------------------- Background runtime kept resident by scripts/start-agent-runtime.sh on a port distinct from llama-server. The registry can define the active ASR model and backend capabilities; these settings remain fallback/default process knobs for the whisper-server binary and idle-model swap.

## Outside the model

Configuration the application cannot see. Most environment variables in `scripts/` are parameters to a single script and belong there.
These are the ones that decide something `Settings` also decides, which means two defaults can disagree.

| Environment | Overlaps | Why it matters |
| --- | --- | --- |
| `LOCAL_AGENT_LLAMA_PORT` | `llama_base_url` | An explicit override. The scripts otherwise take the port from LOCAL_AGENT_LLAMA_BASE_URL, so moving the model's URL moves what they start and stop; 8080 remains the default on both sides. |
| `LOCAL_AGENT_MODEL_REGISTRY_PATH` | `config_dir` | An explicit override. The scripts otherwise derive the registry from LOCAL_AGENT_CONFIG_DIR, the same way Settings.model_registry_path does. |
| `LOCAL_AGENT_START_ASR` | `(none)` | Decides whether ASR exists in the running system. It cannot live in Settings, because the decision is made before Python starts; see the shell flags below. |
| `LOCAL_AGENT_PI_FORCE_DIRECT` | `pi_handoff_to_daemon` | The legacy spelling, still honoured because the handoff docs tell operators to set it around the daemon's Bad file descriptor bug. Inverted: FORCE_DIRECT=1 means pi_handoff_to_daemon is false. |

### Shell flags

Decisions made before Python starts, so they cannot be model fields.

| Script | Flag | What it decides |
| --- | --- | --- |
| `scripts/start-agent-runtime.sh` | `--with-asr / --no-asr` | Start whisper.cpp for this run. Off by default: it holds a multi-gigabyte model resident for the whole session. |

### `.env.example`

Generated by the same script, from the same defaults, so the two cannot disagree. It used to be hand-maintained and had drifted: most variables the scripts read were missing from it, and four contradicted the model.

