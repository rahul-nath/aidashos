apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: local-first-agent-os
resources:
  - namespace.yaml
  - postgres.yaml
  - app.yaml
configMapGenerator:
  - name: postgres-init
    files:
      - init-postgres.sql
generatorOptions:
  disableNameSuffixHash: true
