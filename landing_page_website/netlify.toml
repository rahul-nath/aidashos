[build]
  base = "landing_page_website"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.19.0"

# public/_headers travels with dist for repository builds and manual deploys.
