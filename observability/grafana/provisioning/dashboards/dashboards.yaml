apiVersion: 1

providers:
  - name: local-agent
    orgId: 1
    folder: Local Agent
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
